query CallMaskingProviderList($paging: OffsetPaging) {
  callMaskingProviders(paging: $paging) {
    nodes {
      id
      type
      name
      enabled
      isDefault
      proxyNumber
      createdAt
    }
    totalCount
  }
}
