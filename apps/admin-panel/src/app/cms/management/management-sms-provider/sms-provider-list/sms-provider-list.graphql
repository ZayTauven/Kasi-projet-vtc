query SmsProviderList($paging: OffsetPaging) {
  smsProviders(paging: $paging) {
    nodes {
      id
      type
      name
      enabled
      isDefault
      senderId
      createdAt
    }
    totalCount
  }
}
