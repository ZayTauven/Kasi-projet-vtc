query ShiftRuleList($paging: OffsetPaging) {
  shiftRules(paging: $paging) {
    nodes {
      id
      name
      enabled
      frequency
      maxHoursPerPeriod
      mandatoryBreakMinutes
      enforced
      createdAt
    }
    totalCount
  }
}
