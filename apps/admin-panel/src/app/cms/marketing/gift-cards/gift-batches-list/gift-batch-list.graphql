query GiftBatchList($paging: OffsetPaging) {
    giftBatches(paging: $paging) {
        nodes {
            id
            name
            currency
            amount
            quantity
            availableTimestamp
            expirationTimestamp
            createdAt
        }
        totalCount
    }
}
