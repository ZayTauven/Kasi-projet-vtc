org.gradle.jvmargs=-Xmx3072M
android.useAndroidX=true
android.enableJetifier=true
# Pinned explicitly because the machine's global Flutter JDK config points to a
# non-existent jdk-17.0.6 path. JDK 19.0.1 is the lowest installed JDK that
# satisfies Flutter 3.41's minimum Java 17 requirement for the Android toolchain.
org.gradle.java.home=C:/Program Files/Java/jdk-19.0.1
# AGP 8 disabled BuildConfig generation by default. Several third-party pub.dev plugins
# (e.g. firebase_analytics's android wrapper) still declare custom BuildConfig fields and
# predate this AGP8 default change, so restore the legacy default for all modules.
android.defaults.buildfeatures.buildconfig=true
