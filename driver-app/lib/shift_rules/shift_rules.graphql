query ActiveShiftRules {
    shiftRules(filter: {enabled: {is: true}}) {
        nodes {
            id
            name
            frequency
            maxHoursPerPeriod
            mandatoryBreakMinutes
            enforced
        }
    }
}
